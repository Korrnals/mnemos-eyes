# design/ — арт-дирекция «Живая кора» v2 (vesmaro-eyes)

> **СТАТУС: ЭКСПЕРИМЕНТ. Ветка `experiment/design-neuro-v1`.**
> **Мерж в main ЗАПРЕЩЁН до явного разрешения владельца (фиксация 2026-09-25).**
> Этот каталог — дизайн-канон и спеки прототипа-стенда. Это НЕ продуктовый код:
> `design/docs/` — только спеки и токены; реализация стенда живёт в `design/stand/`
> и строится frontend-разработчиком **по** этим спекам (дизайнер stand/ не создаёт
> и не правит).

## Что здесь лежит

| Файл | Содержание |
| --- | --- |
| `docs/01-CHARTER.md` | Арт-дирекция «Живая кора»: принципы, анти-клише, словарь лора, самокритика направления |
| `docs/02-TOKENS.md` | Полная спека токенов + готовый `tokens.css` (тёмная default + светлая, нейро-слой) |
| `docs/03-IA-NAVIGATION.md` | Навигационная модель стенда: shell, сайдбар, топбар, крошки, палитра Ctrl+K, хоткеи |
| `docs/04-SCREENS.md` | Спеки 8 экранов стенда с состояниями и RU-микрокопирайтом |
| `docs/05-COMPONENTS.md` | Инвентарь компонентов, анатомия и state-матрицы ключевых семи |
| `docs/06-MOTION.md` | Motion-канон: длительности, easing, stagger, ambient, синапс-пульс, reduced-motion |

## Как это потреблять

1. Frontend-разработчик строит `design/stand/` (статический HTML/CSS/JS, без сборки,
   vanilla) строго по `03`–`06`. Токены копируются из блока `tokens.css` в `02` как есть.
2. Имена существующих токенов **заморожены** (ADR 0006, ui-contract §8): нейро-слой
   только **добавляет** имена; переименование/удаление старых запрещено. Значения
   старых имён эволюционировали — см. таблицу «отличия от v1» в `02-TOKENS.md`.
3. Любое отклонение от спеки при реализации — назад к дизайн-лиду, не молча.

## Вне скоупа каталога

- Продуктовые роуты и поведение бэкенда — не здесь (см. `ui-contract.md`, ADR-канон).
- Полный «Атлас»-граф (1k+ узлов, sigma.js) — будущая фаза, в стенд не входит.
- `web/` и `viewer/` в этом worktree не трогаем — это замороженная база для сверки имён токенов.
