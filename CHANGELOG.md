# Changelog

All notable changes to vesmaro-eyes (mnemos-eyes).

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).
Commit lines accumulate under `## [Unreleased]`; version sections appear at
release time (release notes are additionally generated by the GitHub release
workflow — this file is the repo-side trail).

## [Unreleased]

### Added

- **Кора (Kora) — неделя 0, контракт-first: замороженные API-шейпы срезов 1–3 + UI-моки раздела (ADR 0019 rev.2, ратифицирован владельцем 2026-09-24).** Ветвь `feat/kora-week0-contract` замораживает wire-контракт ДО появления серверного кода, по таблице Consequences ADR («Неделя 0 — контракт-first»: OpenAPI-шейпы срезов 1–3 + UI-моки, до кода). (а) Контракт-артефакт `docs/kora/openapi.yaml` — канонический источник истины для срезов 1–3: `GET /api/kora/sessions` (лист производного реестра: executor_id + native_id PK-пара, harness, state live/idle/dead, origin relay/local, steerable, age, preview-кламп ≤160 знаков + блок coverage «что вижу / чего нет»); `GET /api/kora/sessions/{id}/transcript` (единственный transcript-serving путь: seq-курсор `after_seq`/`limit`, каждая запись через redaction choke-point, mnd_-стена = 403 с объяснением, не голый статус); `POST /api/kora/sessions` (новая сессия с телефона — первый релейный сценарий, relay-origin по построению) и `POST /api/kora/sessions/{id}/messages` (промпт через реле: 202 accepted ≠ delivered, честная delivery-статистика «не доставлено — повторите», confirm-режим дефолт, чужой triple → 404 серверно); step-up PIN `POST/GET/DELETE /api/kora/steering/step-up` (TTL ≤ 900с, руление по умолчанию выключено, чтение — без step-up). Производный реестр закреплён расширениями `x-kora-registry` (PK `[executor_id, native_id]`, инвариант `UNIQUE executors.host`, без церемоний регистрации), единый redaction choke-point — `x-kora-redaction` (модуль `server/kora/redaction.py`; всё, что уходит в UI — превью и транскрипты — проходит только через него; аудит `session.transcript_viewed` с device_class/network_path/session_host), SSE-словарь `session.listed/updated/closed` — additive-only, metadata-only (`x-kora-events`; `session.updated` = сигнал chat v1: браузер тянет хвост стора тем же authenticated GET, второго буфера контента на борде нет), `x-kora-no-acp` — сетевого ACP нет, CI-трипвайр духа гейта. (б) Гейт `tests/test_kora_contract_spec.py` (23 теста): эндпоинты/operationId, замороженные enums (harness zcode/vscode/pi без hermes — known-gap до T004; state; origin; коды ошибок), кламп превью, coverage-блок, курсорные параметры, метадата-only структура событий, PK/UNIQUE-host семантика, TTL-кэп 900с, отсутствие ACP-поверхности. (в) UI-моки раздела «Кора» (`viewer/src/features/kora/`) — НИКАКОЙ интеграции с бекендом: сгенерированные из артефакта типы (`koraContract.d.ts`, `npm run codegen:kora` — openapi-typescript над замороженным YAML, committed + drift-guard), gateway-сейм `koraGateway.ts` с единственной реализацией недели 0 — `KoraMockAdapter` (моделирует store-tail семантику §3: send → in-memory стор → seq-курсор перечитывание; step-up PIN гейт; 404 для чужих сессий), экран среза 1 `/kora` (список сессий + экран покрытия «что вижу / чего нет» + онбординг-карточка 3 кейса + честная плашка «Неделя 0 · контракт-моки»), экран срезов 2–3 `/kora/:sessionId` (read-only транскрипт с плашкой «чужая сессия — только чтение» для не-steerable, чат-панель с PIN step-up и send → store-tail для steerable), снапшот-тесты обоих экранов + контракт-тесты фикстур против сгенерированных типов. Раздел жив в навигации сразу (ранний контакт с UI — решение rev.2 #1), маршруты `/kora` и `/kora/:sessionId` в `routes.tsx`, провайдер по конвенции `GatewayContext` (контекст в `.ts`, Provider на route seam). Никакого серверного кода: когда срезы приземлятся, FastAPI `/openapi.json` обязан совпасть с артефактом, а `KoraHttpAdapter` подменит мок одной строкой в одном месте. Verification: pytest 1070 passed / 4 skipped (полный серверный сьют), vitest 1275 passed (161 файл), tsc clean, eslint 0 errors, prettier чист по всем новым файлам.