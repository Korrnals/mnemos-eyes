apiVersion: v2
name: vesmaro-eyes
description: Vesmaro Eyes task board (FastAPI + vanilla JS SPA) with live mnemos integration
type: application
# Both versions are kept in lockstep by scripts/sync-version.sh (single
# source of truth: FastAPI(version=...) in server/app.py) — archcom C5.
version: 1.27.0
appVersion: "1.27.0"
kubeVersion: ">=1.25.0-0"
home: https://vesmaro.abyss.lab
maintainers:
  - name: abyss
    email: abyss@abyss.lab
